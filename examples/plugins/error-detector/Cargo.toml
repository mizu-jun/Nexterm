# Standalone crate: not part of the root workspace (built for wasm32 out of tree).
[workspace]

[package]
name = "error-detector"
version = "0.2.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true
strip = true
